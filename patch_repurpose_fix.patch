--- app/api/repurpose/route.ts
+++ app/api/repurpose/route.ts
@@ -141,7 +141,7 @@
       length,
       flashMode,
       searchDepth: searchDepth,
       maxSearchResults: maxSearchResults,
       imageRequest
-    }, proxiedModel);
+    }, proxiedModel); // The orchestrator uses the provided platforms internally. We need to pass executableChannels!
