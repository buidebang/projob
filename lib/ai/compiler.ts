export class SEOCompiler {
  public static compile(rawContent: string, platform: string, injectedRules?: string): string {
    // Rigid templates removed in favor of organic AI-generated structural output.
    // The AIGateway's baseSystemPrompt now natively handles platform-specific voice.
    return rawContent.trim();
  }
}
