--- app/(protected)/dashboard/page.tsx
+++ app/(protected)/dashboard/page.tsx
@@ -194,8 +194,10 @@
       }

       let resultText = "";
-      if (data.outputs && data.outputs[selectedPlatforms[0]]) {
-        resultText = data.outputs[selectedPlatforms[0]].textContent;
+      setGeneratedOutputs(data.outputs || {});
+      const firstKey = Object.keys(data.outputs || {})[0];
+      if (firstKey && data.outputs[firstKey]) {
+        resultText = data.outputs[firstKey].textContent;
       }

       let i = 0;
@@ -208,7 +210,7 @@

       const newHistoryNode: SessionHistoryNode = {
         id: crypto.randomUUID(),
-        platform: selectedPlatforms[0],
+        platform: selectedPlatforms.join(', '),
         inputText,
         timestamp: new Date().toISOString()
       };
@@ -374,7 +376,7 @@
                         <div className="flex items-center justify-between px-2">
                           <div className="flex items-center gap-3">
                             <span className="text-[10px] font-black uppercase tracking-widest text-slate-500 bg-slate-900 border border-slate-800 px-2 py-0.5 rounded-full flex items-center gap-2">
-                              <Sparkles size={13} className="text-cyan-400 animate-pulse" /> {selectedPlatforms[0]} Optimization Matrix
+                              <Sparkles size={13} className="text-cyan-400 animate-pulse" /> {platform} Optimization Matrix
                             </span>
                             <span className="text-xs font-bold text-emerald-400 font-mono">
                               ✓ Yield Complete
@@ -466,7 +468,7 @@
                 value={inputText}
                 onChange={(e) => setInputText(e.target.value)}
                 onKeyDown={(e) => { if (e.key === "Enter" && !e.shiftKey) { e.preventDefault(); handleExecuteOrchestration(); } }}
-                placeholder={`Ask ProJob to synthesize keywords & generate context for ${selectedPlatforms[0]}...`}
+                placeholder={`Ask ProJob to synthesize keywords & generate context for ${selectedPlatforms.join(', ')}...`}
                 className="w-full bg-transparent text-xs text-slate-200 placeholder:text-slate-600 focus:outline-none resize-none p-2 max-h-24 h-12 leading-relaxed text-left"
                 style={{ direction: "ltr" }}
               />
